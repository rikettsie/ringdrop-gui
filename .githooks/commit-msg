#!/bin/sh
set -e

subject=$(head -1 "$1")
len=$(printf '%s' "$subject" | wc -c | tr -d ' ')
if [ "$len" -gt 72 ]; then
    echo "error: subject line is $len characters — must be 72 or fewer"
    echo "  $subject"
    exit 1
fi
